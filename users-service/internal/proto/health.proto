syntax = "proto3";

package health;

option go_package = "github.com/username/reponame/users-service/internal/proto;proto";

service HealthService {
    rpc CheckHealth(HealthRequest) returns (HealthResponse);
}

message HealthRequest {}

message HealthResponse {
    string message = 1;
}
